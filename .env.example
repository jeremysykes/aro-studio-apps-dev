# ─── Tenant Configuration ─────────────────────────────────────────────────────
#
# Config resolution order:
#   1. Zod defaults  (every field has a sensible default)
#   2. tenant.config.json  (optional structured config at project root)
#   3. Env var overrides   (this file — highest priority)
#   4. Zod validation      (fails fast on invalid values)
#
# Your .env values ALWAYS override tenant.config.json. If you delete a var
# below, the value falls through to tenant.config.json, then to defaults.
# See docs/architecture/TENANT_CONFIGURATION.md for full reference.
# ──────────────────────────────────────────────────────────────────────────────

# UI model: standalone | sidebar | dashboard | tabs | carousel
ARO_UI_MODEL=standalone

# Enabled modules (comma-separated). Used by all UI models.
ARO_ENABLED_MODULES=inspect

# Workspace root: the project directory Inspect operates on.
# Defaults to the aro-studio-apps-dev project root if not set.
# ARO_WORKSPACE_ROOT=/path/to/your/project

# Optional: pre-populate Inspect Setup tab (Vite client env)

# Figma:
# VITE_FIGMA_TOKEN=figd_…
# VITE_FIGMA_FILE_KEY=… or VITE_FIGMA_FILE_URL=https://www.figma.com/design/…

# Inspect module:
# VITE_STORYBOOK_INDEX_URL=https://…
# VITE_STORYBOOK_INDEX_PATH=
# VITE_CODE_TOKENS_PATH=mocks/tokens.json
